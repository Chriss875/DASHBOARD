# ==========================================
# Environment Configuration for Docker
# ==========================================
# Copy this file to .env and customize values
# cp .env.template .env

# Database Configuration
DB_ROOT_PASSWORD=changeme_root_password
DB_DATABASE=tjpsd32
DB_USER=ojsuser
DB_PASSWORD=changeme_db_password

# Application Configuration
SERVER_PORT=8080
# Generate a secure API key or use your existing one
APP_API_KEY=your_api_key_here_replace_with_actual_key

# CORS Configuration (comma-separated URLs)
CORS_ALLOWED_ORIGINS=http://localhost:4200,http://localhost:3000,http://localhost:8080
